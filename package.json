{
  "name": "alert-io",
  "version": "1.0.0",
  "private": true,
  "description": "Alert-IO stroke risk monitoring MVP",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "mobile": "pnpm --filter mobile start",
    "build": "pnpm --filter web build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
